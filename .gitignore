node_modules/
dist/
*.log
.DS_Store
server/data/*.json

# 环境变量（密钥只走环境/后台配置，禁止入仓）
.env
.env.*
!.env.example
!.env.docker.example

# 上传文件与运行时数据
uploads/

# 归档：源码入库；依赖与构建产物沿用上方 node_modules/、dist/
# archive/admin-pc 仅为本地构建残留（正式源码在仓库根 admin-pc/）
archive/admin-pc/
client_archive_tmp/

# docker 镜像离线包（可重新 docker save 生成，不入仓）
archive/deploy/hof-image-package/hof-images.tar.gz

# 后端运行时输出
server/coverage/
server/seeds/ihg-opportunities.json
server/seeds/import-ihg.js

# AI 编码工具本地数据
.zcode/
